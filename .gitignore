# Python bytecode
__pycache__/
*.pyc
*.pyo
*.pyd

# Environment
.env
.venv/
venv/
env/

# Distribution / packaging
dist/
build/
*.egg-info/
*.egg

# Testing
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Compiled extensions
*.so
*.dylib

# Uploads (user-submitted flier images)
uploads/

# Logs
*.log

# ChromaDB local data
data/chromadb/

# SQLite feedback database (runtime data)
data/feedback.db

# Transcripts — ignore all except the RA Exchange interview
# transcripts which are included in the Docker build for auto-ingest.
transcripts/*
!transcripts/ra_exchange/
# Ignore progress/log files within ra_exchange
transcripts/ra_exchange/_*.json
transcripts/ra_exchange/_*.log

# Book text files (user-provided, potentially copyrighted)
# Keep the directory structure (.gitkeep) but ignore content files
data/books/*.txt
data/books/*.pdf
data/books/*.epub

# RA.co scrape checkpoints (large JSON files, resumable)
data/ra_scrape/

# SQLite flier history database (runtime data)
data/flier_history.db

# Scrape logs
scrape_log.txt

# Node modules (Remotion presentation project)
node_modules/

# SQLite session state database (runtime data)
data/session_state.db

# Discogs data dumps (10+ GB XML files, downloaded separately)
data/discogs_dumps/

# Discogs corpus (generated from dumps, ingested into ChromaDB)
data/discogs_corpus/
